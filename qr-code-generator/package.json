{
  "name": "qr-code-generator",
  "version": "1.0.1",
  "description": "Generate QR codes from text, URLs, and data with customizable options",
  "main": "src/runner.js",
  "scripts": {
    "dev": "netpad-plugin dev",
    "build": "netpad-plugin build",
    "test": "jest",
    "validate": "netpad-plugin validate",
    "publish": "netpad-plugin publish"
  },
  "keywords": [
    "utility",
    "netpad",
    "plugin"
  ],
  "author": "NetPad Team <team@netpad.io>",
  "license": "MIT",
  "devDependencies": {
    "@netpad/plugin-cli": "^1.0.0",
    "jest": "^29.0.0"
  },
  "peerDependencies": {
    "@netpad/core": "^1.0.0"
  }
}
